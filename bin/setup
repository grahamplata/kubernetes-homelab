#!/usr/bin/env bash
set -euo pipefail

# Apply git hooks
./bin/sync-hooks

# Install required tools
./bin/install

cat <<ReadMe

MOTD:
  Your Machine is now setup
    - Make sure to take out the trash
ReadMe
